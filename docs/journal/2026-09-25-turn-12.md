# Turn 12: ranked frontier and the remaining 2003 corner

## Frontier ranked by evidence

Order: reviewed semantics with a concrete theorem or model route; reviewed semantics with bounded evidence only; source-fidelity questions needing an independent primary reading. Within a tier, an exact-claim collision verdict of `none-found` precedes `partial`, which precedes absent or unverified; ties use Q number. `partial` is not a novelty verdict. Q-011 is the primary workstream irrespective of its rank. Q-010 repeats its remaining set and is not independent. Q-008's one-fork part is proved; Q-005/Q-006/Q-012/Q-013 are closed.

| Rank | Question and remaining claim | Best current certificate | Missing obligation | Checked-literature status |
|---|---|---|---|---|
| 1 | Q-002: all five 2000 conditions with same-number completeness | Bounded escape; Critical-Band candidate | Exact all-size axiology and firsthand source check | R2: partial; related Critical-Band work known, exact five-condition conjunction unverified |
| 2 | Q-008: parameterized multi-fork minimal motif classification | P22 proves one-fork necessity and decides each finite branch; four-edge two-fork example | All-length multi-fork family and minimality converse | P22: partial; rank-cycle criterion standard, multi-fork classification unchecked |
| 3 | **Q-011 (primary):** unrestricted 2003 NE with thesis not-worse DA | P23 chosen-witness contradiction; P21/P23 models for different weakened sets | All legal witnesses for impossibility, or an all-profile model at one legal witness | P18/P20/P21/P23: partial; original five-condition theorem known, exact corner's priority unverified |
| 4 | Q-001: avoidance comparability suffices instead of completeness for the 2000 theorem | Propositional proof and witnesses for every (p,q) | Audit source applicability for every witness and exact Addition polarity | R3 collides on broad positive avoidance; exact local comparability has no verified collision verdict |
| 5 | Q-003: minimum escape isolates the Sadism/Anti-Egalitarianism junction in every Arrhenius-type skeleton | Minimum escape verified for baseline, R6 variants and a relaxed instance | Quantification over every motif-census class | R1: partial; minimum-location claim unchecked |
| 6 | Q-007: priority triangle firing condition at every size | P10's 108-witness finite census; 24 fire | Necessary/sufficient count theorem for unbounded sizes | No exact-claim collision verdict; literature unchecked |
| 7 | Q-009: exchange-rate characterization of repeated δ/β steps | Five-edge δ²–ED–β² example and bounded no-cycle controls | Every-length rate theorem and witness families | No exact-claim collision verdict; novelty unknown |
| 8 | Q-004: category gap in the 1999/2000 conditions | Order-only R5 triangle; thesis ranges resolve only the later family | Source-justified gap axiom for 1999/2000 | R5: partial; precise source gap unresolved |
| 9 | Q-014: indexed-chain versus incomparable off-chain levels | P24 indexed-domain restriction; inserted and incomparable probes | Source indexing/mixed-background reading and an audited extended generator | P24: partial; source does not resolve extra-level scope |

## Corrected scanner and conditional obstruction

P23's forward GNEP branch used one high and one low life even when its validated witness had `n=2`; its VRC branch used the first high level without requiring every source life to have that level. Those edges are false source applications. The repaired scanner removes `n` high lives and the `W_z` life as a multiset, tries every low bag of size `n`, and requires VRC's source to be perfectly equal. Exhaustive populations through three lives over W_-1..W_6 give exactly the ED/NE/GNEP/VRC instance sets from `ladder.instances_over` at both `(gnep.n,vrc.n)=(1,1)` and `(2,2)`; every enumerated instance passes the independent `ladder.audit`. The original P23 ledger IDs remain historical. Its regenerated artifact is the operative census: at cap eight, P21 and the phase-17/18/20 witness still have singleton obstructions; `ne-n2`, `gnep-n2`, `vrc-m2`, `vrc-n2` have none. In particular an old `gnep-n2` path through the miscounted GNEP edges cannot be cited. This is a correction of a diagnostic, not a proof of consistency.

For every integer r≥1, let a<a'<b and c>0, k≥1. If primitive paths give `W_a^r ⪰ T` and `W_a'^r ⪰ T`, with `T=W_b^r ∪ W_c^k`, the thesis DA clause for `A=W_a'^r`, `B=W_b^r`, `C=W_c^k` gives `¬(A ≻ T)`. Since `A ⪰ T` already holds, the definition of strict preference forces `T ⪰ A`. Thus `W_a^r ⪰ W_a'^r`, contradicting strict ED `W_a'^r ≻ W_a^r`. Only reflexivity, transitivity and the strict part of the weak relation are used; completeness is not. This is an **all-r conditional lemma**, not an every-witness construction of its two paths. The retained r=1 P23 target `(1^7,6)` is independently replayed, audited, and UNSAT in `research.lab.Engine`. For r=2, `A=(3,3), B=(6,6), C=(1,1)` passes the thesis DA audit; replacing one C life by 2 makes the sole remaining addition mixed and fails it.

| Witness | r searched | Cap 8 | Cap 10 |
|---|---:|---|---|
| P21 fixed witness | 1 | Audited target `(1^7,6)`; exact UNSAT | Same target; exact UNSAT |
| `ne-n2` | 1 | No common DA target | No common DA target |
| `vrc-m2` | 1 | No common DA target | No common DA target |
| `vrc-n2` | 1, 2 | No common DA target for either r | No common DA target for either r |

Each row fixes the named legal NE/GNEP/VRC parameters on W_-1..W_6, searches from all equal populations of its stated size at every indexed level, and labels absence `not_a_general_proof=true`. The r=2 row is a separate target-decomposition check, not a result obtained by treating VRC's two unequal high lives as one source. Every reported path edge passes P18/P19 replay and `ladder.audit`; the finite relata close without completeness in `Engine`.

## Universal routes and exact scope

The P23 singleton path cannot be mechanically parameterized at `ne-n2`: its NE step consumes two equal middle lives for `n=1`, whereas this legal witness requires three. The source allows NE's n to depend on the pair of levels; GNEP's high floor/range/count to depend on z; and VRC's two ranges, negative level and counts to be chosen separately. The finite controls do not supply the missing count identities or a source-grounded chain for arbitrary values. No five-condition impossibility is promoted.

For the `ne-n2` **candidate** least primitive preorder, an all-finite-path acyclicity argument does go through on the chosen indexed chain: P21's strictly positive decreasing adjacent gains make ED, unrestricted NE with n=2, and GNEP with `(u,y,n)=(5,3,1)` strictly decrease the sum potential on every size-preserving step; VRC at `(x,u,v,y,n,m)=(-1,4,6,3,1,1)` only grows size when its frozen low bag is nonempty. Exact SMT checks the NE and GNEP inequalities; Spacer excludes a finite path that returns to its original size without a potential decrease. The cap-ten singleton closure has no reachable DA target at this witness; a larger VRC bag remains unbounded, and **no symbolic DA-forbidden-return invariant** for every finite profile has been established. Acyclicity alone does not make the candidate a model. Q-011 stays open. Empty VRC bags and empty DA additions/sources are not silently included in either scanner or theorem.

## Prior work and fidelity gate

The 2003 primary paper (printed pp. 171–177) states the original conjunction with **positive** DA, unrestricted NE backgrounds and VRC avoidance, then derives β and δ and closes a strict cycle. The thesis (p. 159) states **N-shaped not-worse** DA with a perfectly equal positive C; its ranged NE (p. 166) is not the unrestricted 2003 NE. The published 2001 lemma chain (pp. 33, 41–43) uses Weak Non-Sadism and Weak Quality Addition rather than this DA/VRC pair. The 2016 reconstruction (manuscript pp. 4–5, 10) distinguishes negative and positive DA, but its weak-DA conclusion `¬(low ⪰ high)` is not a negation of 2003 VRC avoidance `high ⪰ low` in an incomplete preorder. P18/P20/P21/P23 have different fixed witnesses, weak/positive DA forms, or all-size model claims. The checked work does not establish the P25 conditional target or an every-witness proof; independent publication of an equivalent conditional argument is **unverified**, not `none-found`. The later book and other inaccessible full texts listed in the P21/P23 collision entries remain unread. No priority claim.

An unsent expert inquiry would ask: on 2003 p. 172, does unrestricted `B ⊂ R(1,y)` include `B=∅`, and on thesis p. 159 do the unrestricted sizes allow `C=∅` or `A=B=∅`? The frozen scanner says no in both cases; the literal readings may say yes. Admitting them adds VRC `W_z^n ⪰ W_x^m`, zero-addition DA and possibly `∅` comparisons, changing the model obligation. Any stronger claim needs a superseding formalization decision, two-agent source cross-read and independent checker. No correspondence was sent.
